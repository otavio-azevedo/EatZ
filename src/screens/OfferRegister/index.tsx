import { Text } from 'react-native';

export default function OfferRegisterScreen({ navigation, route }) {
  //TODO: Implementar tela de cadastro de ofertas
  console.log('OfferRegisterScreen' + JSON.stringify(route.params));
  return <Text>OfferRegisterScreen</Text>;
}
